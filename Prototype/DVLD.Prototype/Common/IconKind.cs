namespace DVLD.Prototype.Common
{
    /// <summary>
    /// The full icon set used across the prototype. Every icon is drawn in
    /// code by IconGlyph (simple flat geometric strokes) rather than loaded
    /// from bitmap/font assets — see STYLE_GUIDE.md "Icon set" for the
    /// concept-to-glyph rationale and the full list mapped to screens.
    /// </summary>
    public enum IconKind
    {
        Person,
        PersonAdd,
        People,
        IdCard,
        Mail,
        Phone,
        Address,
        Calendar,
        Globe,
        Gender,
        Car,
        Search,
        Money,
        Lock,
        Test,
        History,
        Warning,
        Close,
        Save,
        Edit,
        Delete,
        Check,
        Error,
        Info,
        SignOut,
        ChevronRight,
        Filter,
    }
}
